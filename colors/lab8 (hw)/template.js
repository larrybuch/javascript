$(function() {

$('#add_colors').click(add_colors);

});

function add_colors ()
{
	//get colors
	var colors = $('#input').val();

	//make array
		colors = colors.split(', ');

	//figure out how many colors
		
}